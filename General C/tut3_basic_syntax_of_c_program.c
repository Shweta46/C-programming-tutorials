#include<stdio.h>
int main(int argc, char const *argv[])
{
    int a, b; 
    //example of tokens
    printf
    (
        "Enter number a\n"
    );
    scanf
    (
        "%d", &a
        )
        ;
    //readable and compact program line
    printf("Enter number b\n");
    scanf("%d", &b);    
    printf("Sum is %d\n", a+b);
    return 0;
}
//a c program is made up of tokens that its basic building blocks
//keywords : they are reserved words used for general purpose in C, there are total of 32 keywords in C
//Identifiers: any name that we want to give to a function or variable or constants etc.
//it is a case sensitive language
// &a is a type of symbols
// Program is made up of keyword, identifier, constant, and string literal
//